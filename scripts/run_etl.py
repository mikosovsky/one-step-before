# Cel: uruchamialny skrypt uruchamiający pipeline.
# Zachowanie:
# parsuje args (konfig), ładuje configs/default.yml, inicjuje logger, wywołuje ETLPipeline.run() i zapisuje wynik.
# Exit codes i logowanie.